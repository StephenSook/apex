# APEX - Pre-Mortem Journal

> A running journal of failure modes and mitigations. Started Day 2 per PLAN.md task 1.7. Pulled forward to Day 1 EOD per the galaxy-tier "everything ships" rule. Updated daily. Stephen owns the journal entry. Vinh contributes to backend / data / infra rows.
>
> Mirror of the Hometown convention `docs/build_audit_2026-05-03.md` pattern. Every failure mode logged. Every mitigation documented. Day 11 final polish becomes the Q&A defense reference.

---

## How to use this journal

Each row: `[Day] [Surface] [Failure mode] [Why it happens] [Mitigation] [Status]`.

Status legend: 🟡 actively mitigating · ✅ mitigation shipped · ⚠ accepted residual risk · ❓ unknown (not yet hit).

---

## Day 1 (2026-05-20) - observed failures

| # | Surface | Failure mode | Why it happens | Mitigation | Status |
|---|---------|--------------|----------------|------------|--------|
| 1 | Terminal paste | zsh interpreted line-wrapped clipboard paste as separate commands, breaking `gh repo create` chain twice | iTerm2 default soft-wraps long commands; pasted text retained newlines from markdown code block formatting | Tightened command form to single-line, then ran via Bash tool directly to bypass terminal paste | ✅ |
| 2 | Em-dash in prose | 2 em-dashes shipped to `docs/decision-log.md` from earlier writes | Default Claude prose habit | Manual `grep -rn "—"` sweep on Day 1 PM caught both, replaced with `.` + `:` substitutes | ✅ |
| 3 | Tool naming drift | "Granite Instruct" named 4 ways across files | Wrote the name from memory each time instead of pinning canonical form | Canonicalized to "Granite 4.1 8B Instruct" in prose; badge UI keeps abbreviated form | ✅ |
| 4 | Version drift | "Next.js 15" referenced 3 places, actual is 16.2.6 | PLAN.md written before `create-next-app` ran; never updated post-scaffold | Grep-and-replace across README + PLAN | ✅ |
| 5 | Code review lane | First attempt was Claude reviewing Claude's own frontend | Forgot the global three-brain HARD RULE on self-review | Routed to `codex:codex-rescue` + `pr-review-toolkit:code-reviewer` parallel | ✅ |
| 6 | Vinh-lane interference | About to scaffold `app/backend/` Python without Vinh's input | Galaxy-tier "everything ships" reflex overrode ownership table | Stephen flagged "don't interfere with Vinh" → backed off backend, kept to Stephen-lane only (frontend polish + docs + narrative + outreach) | ✅ |
| 21 | Filename privacy leak | A wave-7 outreach-draft filename embedded a supplier identity (operator name + slug). Body-only sweeps did not catch it. | Sweep tooling only inspected file contents, not paths. | Renamed to `adaptive-supplier-consent-day-1.md` + body sanitized. Added explicit operator-name + path check in `scripts/pre-submit-checks.sh` Check 4. Canonical filename pre-rename tracked in private memory. | ✅ |
| 22 | Operator deducibility from context | Public consent draft still narrowed supplier to a tiny set via "simultaneity-permitting hand-control technology you ship" | Anonymization swept the proper noun but not the unique capability description that fingerprints the supplier | Codex wave-8 review caught it. Replaced with generic "adaptive hand-controls of the class you supply." | ✅ |
| 23 | Personal email leak in public draft | The IBM Consulting cold-email draft carried Stephen's personal email in three slots; two LinkedIn DM drafts carried Stephen's school email in the signature block. | Anonymization protocol applied to recipient identities but not to sender identities. | Redacted to `[personal email kept private]` / `[school address kept private]`. Canonical sender addresses now in private memory only. | ✅ |
| 24 | Commit-count claim drift | `STATUS_DAY1.md` said 77+ atomic commits while `git log --oneline | wc -l` returned 87 | Sweeps updated the claim without re-counting from git | Codex wave-8 review caught it. Bumped to 87+. Added implicit lesson: regenerate counts from `git log` each time. | ✅ |
| 25 | AI-tone regex false negatives | First-pass `pre-submit-checks.sh` regex was trailing-space-anchored + case-sensitive + missing the wider blocklist (empower, intuitive, elevate, transform, etc.) | Initial regex used the literal CLAUDE.md row order without word-boundary anchors | Rewrote to case-insensitive `\b…\w*` patterns. Scoped to prose files only so Tailwind classes like `transition-transform` no longer false-positive. | ✅ |
| 26 | ESLint environmental break | `pnpm exec eslint` throws `TypeError: LazyLoadingRuleMap is not a constructor` after Node 22.22.2 reinstall | Likely Node module-resolution caching + ESLint 9.39 internal API drift | `tsc --noEmit` + `next build` continue to gate correctness; both verified clean Day 1 EOD. Lint check distinguishes binary crashes (soft-fail with note) from real lint findings (hard-fail) in pre-submit-checks.sh Check 8. Day 2 task: pin ESLint version or migrate to `next lint`. **Partial mitigation only**: tsc covers types + syntax but does not catch React hook misuse, react/jsx accessibility, or import-order regressions. Day 2 ESLint repair is a hard Day-2 task, not a deferrable nice-to-have. | 🟡 |
| 30 | Research recon PDFs leaking stakeholder identity | 9 Day-0 model-recon PDFs (Gemini / Claude / Perplexity / DeepSeek / Groq / Kimi / 2x ChatGPT / physics) committed to public repo at `research/` carried operator names + emails + phone numbers in their content. Binary grep on PDFs returned hits that text-only sweeps missed | wave-7 anonymization sweep grepped only file contents, not binary PDF text via `pdftotext` | Codex wave-11 review caught it. Deleted 9 leaky PDFs from public HEAD, preserved local copies in Obsidian Imports + private memory, added to `.gitignore`. Official IBM source PDFs (`ibm-rules.pdf`, `may-challenge-ibm.pdf`) stay tracked. Git history retains the deleted PDFs (residual risk). | ✅ |
| 31 | Private denylist embedded in public script | `scripts/pre-submit-checks.sh` Check 4 regex literally listed all the operator names + personal emails it was meant to catch, publishing the watchlist itself | Convenience: keeping the regex inline in the script that uses it | Moved denylist to `scripts/private-denylist.txt` (gitignored). Script reads patterns via `grep -F -f` with env-var override (`APEX_PRIVATE_DENYLIST`). Shipped `private-denylist.txt.example` as the template. Codex wave-11 BLOCKER. | ✅ |
| 32 | Dropzone empty-file silent accept | `validateTelemetry()` + `validateCoa()` accepted 0-byte files because the size check used `>` (not `>=` against a minimum floor). User picks corrupt/empty file, slot turns green, backend rejects with cryptic error | Validators only enforced upper bounds, not lower | Wave-12 added `if (file.size === 0)` early-return + `MIN_TELEMETRY_BYTES = 1024` floor on telemetry. Silent-failure-hunter B1. | ✅ |
| 33 | Dropzone multi-file drop silent discard | `event.dataTransfer.files?.[0]` silently dropped files 2..N on a multi-file drop; folder drops also fell through | Drop handler assumed single-file invariant | Wave-12 added explicit `files.length > 1` rejection + `webkitGetAsEntry().isDirectory` rejection with clear error in the slot's error region. Silent-failure-hunter B2. | ✅ |
| 34 | Dropzone double picker open via label htmlFor + role=button onClick | The `<label htmlFor={inputId}>` AND the `<div role="button" onClick={openPicker}>` both fired the picker, racing each other | Patterns merged a standard form-label affordance with a custom drag-and-drop container | Wave-12 moved the `<input>` outside the clickable region as a sibling, replaced `<label htmlFor>` with `<span id={labelId}>` + `aria-labelledby={labelId}` on the role=button div. Added `tabIndex={-1}` + `aria-hidden` to the sr-only input. Codex wave-11 HIGH 1+2+3. | ✅ |
| 35 | Dropzone SlotState allowed contradictory states | `{ file: someFile, error: "bad", isDragging: true }` was a representable shape; the reducer was the only invariant guardian | Plain object shape with three independent fields | Wave-12 refactored to a discriminated union (`status: idle | drag | filled | error`) so contradictory shapes are unrepresentable. Render branches narrow on `status`. Type-design B2. | ✅ |
| 36 | Dropzone field-name divergence from backend Pydantic schema | `DropzoneSubmission { telemetry, coa, driverId }` vs `AnalyzeRequestPayload { telemetry_csv, coa_pdf, driver_id }` would have required a manual remap at the fetch boundary | Casing convention mismatch (TS camelCase vs Python snake_case) without a centralized mapper | Wave-12 renamed `DropzoneSubmission` fields to snake_case matching `AnalyzeRequestPayload` verbatim. No remap needed. Type-design B1. | ✅ |
| 37 | Pre-submit script silent failures (gh + jq + grep + flag parsing) | `gh run view` failure fell back to `'{"jobs":[]}'` → silent PASS; `jq` parse error fell back to empty string → silent PASS; grep errors swallowed by `\|\| true`; unknown flags + flag conflicts silently ignored | Defensive fallbacks masked real verification failures | Wave-12 hardened: `set -o pipefail` + `trap cleanup EXIT` + flag validation (unknown flag → error, --soft+--final conflict → error, --only=abc → error) + PROSE_PATHS existence pre-filter + gh/jq failure → HARD-FAIL not PASS + ffprobe missing → HARD-FAIL in `--final` mode. Silent-failure-hunter H2-H6 + Codex wave-11 HIGH 4. | ✅ |

## Day 2 (2026-05-21) - observed failures

| # | Surface | Failure mode | Why it happens | Mitigation | Status |
|---|---------|--------------|----------------|------------|--------|
| 38 | pnpm node_modules cache corruption | `eslint-config-next/package.json` was an empty file after a series of `pnpm install` runs during wave-12 (when `.git/index.lock` fighting forced repeated installs). Manifested as "Invalid package config" + "LazyLoadingRuleMap is not a constructor" Node module-resolution errors | pnpm's symlinked .pnpm cache + concurrent install attempts can leave per-package package.json files truncated | Wave-13 fix: nuke `node_modules` + `pnpm-lock.yaml` entirely, then fresh `pnpm install`. ESLint 9.39.4 runs cleanly post-reinstall + caught 2 real lint findings on Dropzone + analyze page. Pre-mortem row 26 🟡 → ✅. | ✅ |
| 39 | Internal-nav `<a href="/">` instead of `<Link>` | First-draft `app/analyze/page.tsx` breadcrumb used a raw `<a>` for the APEX home link, bypassing Next.js's client-side navigation | Habit from non-Next codebases | Caught by ESLint `@next/next/no-html-link-for-pages` rule once the env was repaired. Replaced with `<Link href="/">` from next/link. | ✅ |
| 40 | Unused `SLOT_KEYS` const carryover from wave-12 refactor | Added `SLOT_KEYS = ["telemetry", "coa"] as const` planning to iterate it for exhaustiveness checks, but never actually used it iteratively | Refactor residue: kept the helper without the use case that motivated it | Caught by ESLint `@typescript-eslint/no-unused-vars` rule. Collapsed to the bare type literal `"telemetry" \| "coa"` since the const was unused. | ✅ |

## Forward-looking failure modes (anticipated)

| # | Day | Surface | Failure mode | Why | Pre-emptive mitigation |
|---|-----|---------|--------------|-----|------------------------|
| 7 | 1-2 | Vinh-side onboarding | Vinh installs npm/pnpm but his node version differs (currently node@22 on Stephen's box) | Node version drift across team | `app/frontend/.nvmrc` Day 2 + Vinh confirms node version in his TTM smoke log |
| 8 | 2 | Granite-Docling on real COA PDF | Granite-Docling 258M cold-start may take 10+ minutes on first parse; non-Latin scripts may fail | Granite-Docling is a 258M VLM with limited multilingual training data | Cache the parsed JSON at onboarding; pre-test on English-only fixture first; fallback to manual JSON if first FIA COA fails |
| 9 | 3-5 | CvxpyLayer QP non-convergence | Friction-ellipse + bicycle-model + COA-simultaneity constraints may produce infeasible solutions on edge-case telemetry | QP needs interior feasibility; some forecast traces will require constraint relaxation | V1: catch and log infeasibility, fallback to prior-lap baseline; V2: relax μ bound with explanation in Guardian audit |
| 10 | 4 | TTM zero-shot baseline worse than seasonal-naive | TTM was pretrained on weather + retail, not motorsport | Domain shift | If Gate G4 fails on 3+ of 5 holdout circuits, reframe pitch from "TTM forecasts pace" to "TTM forecasts envelope" + soften deck claim |
| 11 | 5-6 | Granite Guardian false-positive on legitimate engineering language | Guardian's safety classifier may flag "reduce brake force by 12%" as unsafe-recommendation | Guardian's policy boundaries vs engineering domain language | Convergence 14 serializer unit-test suite catches this; surface Guardian reasoning trace in UI; allow human-engineer override on flagged recommendations |
| 12 | 3-5 | Phase 1+2 stakeholder emails go dark | UK charity / nonprofit response times are typically 7-14 business days | Outreach is a slow channel | **Day 3 escalation** (revised from Day 7 per Codex critique #4): LinkedIn DMs to two UK adaptive-driver competitors. Day 5 second-pass follow-up emails to non-responders. Day 8 anonymization fallback decision if zero replies. |
| 13 | 8 | Demo loop > 60s on commodity hardware | DeepSeek engineering audit warned 15-32 min on CPU + 60s only on RTX 4060 | Granite-Docling + Guardian + Instruct end-to-end | Aggressive pre-cache of document parses at onboarding; live 60s budget is ONLY TTM + projection + Guardian + Instruct narration |
| 14 | 9 | Sim-rig (iRacing / ACC) crashes during 3-min video recording | Live sim-rig is the most fragile element of the demo | iRacing / ACC are not designed for live data export to external WebSocket | Record fallback video on Day 9 first thing; if live mode crashes during Day 10 production take, ship the canned replay |
| 14b | 9-10 | HF Space cold-start during judge eval | Free-tier HF Spaces sleep after 48h of inactivity | HF Space economics | Keep-alive cron during May 28-31 judging window; Colab notebook as zero-install fallback |
| 15 | 10 | Video file size > BeMyApp upload limit | 3-min 1080p H.264 with voice overdub can hit 200+ MB | Submission portal upload caps vary | YouTube unlisted host instead of direct upload; the form accepts URL |
| 16 | 11 | AI-tone sweep finds last-minute em-dash drift | Late Day 10 polish edits may reintroduce em-dash via mechanical autocorrect | Habit + autocomplete | `scripts/ai-tone-sweep.sh` runs on Day 11 morning + before final commit |
| 17 | 11 | Operator-attribution violation (named-pre-consent risk) | Marketing copy may name suppliers + community without explicit per-surface consent | Q-006 open question | Day 1 EOD anonymized supplier + adaptive-racing-programme names across 12 public files pre-emptively; revert names on per-surface consent reply; final-state anonymized by Day 10 if no replies |
| 18 | 12 | BeMyApp form submission rejected (wrong field format / missing field) | Form validation rules not fully known until submission attempt | Submission portal idiosyncrasies | Submit Day 12 14:00 ET (7-hour buffer before 11:59 PM ET deadline) leaves time for fixup |
| 19 | 12 | Hostile Q&A question outside the 5 prepared flashcards | Judges may ask something unanticipated | Q&A is high-variance | Flashcard #6 (catch-all): "We deliberately scope-limited APEX V1 to the COA + TTM + physics layer pattern. The question you're asking is genuinely open and a great Day 13 conversation - happy to dig in." |
| 20 | 12 evening | Submission timestamp on BeMyApp form differs from intended (timezone confusion) | BeMyApp portal may display UTC vs ET | Timezone math | Verify on Day 11 dress rehearsal; screenshot submission timestamp |
| 27 | 2 | Dropzone CSV parse fails on FastF1 channel naming | FastF1 column names vary by year + provider; my Dropzone assumes 8 channels exactly | Channel-mapping mismatch between FastF1 export + Vinh's TTM input adapter | Day 2: agree column-mapping table with Vinh in `app/shared/types.ts` comment; Day 3: add CSV-header preview row before submit. Until then the Dropzone accepts any CSV shape and defers schema validation to backend. |
| 28 | 2 | Tab-order regression in `/analyze` when Vinh's coaching report mounts below the form | Once the backend returns and a report renders, focus may jump back to top of page | React reconciler default behavior | Day 5-6: scroll-into-view + focus management on result mount (move focus to result heading, not body top). Currently Day 1 EOD ships dropzone only. |
| 29 | 2-10 | Per-submit-checks.sh false positives or false negatives drift as the prose surface grows | Adding new doc files without updating allowlist | Allowlist is conservative; new patterns slip through grep | Re-run script after every wave; Codex review on the script itself before Day 11 final pass. Pattern proven Day 1 EOD where Codex wave-8 caught 7 bugs in the initial 215-line script. |
| 41 | 2-5 | Backend Pydantic schema drift from frontend `app/shared/types.ts` | Vinh's `app/backend/apex/schemas.py` mirrors a 12-type contract by hand. Any field rename, type change, or new field in `types.ts` must be mirrored exactly OR the multipart POST returns 422 | Cross-language schema sync without codegen | Wave-12 tightened `AnalyzeRequestPayload.telemetry_csv` to `File` only (no `Blob` union). DropzoneSubmission field names now match Pydantic snake_case verbatim. `⚠️ CONTRACT` commit prefix enforced per PLAN.md §Coordination Protocol rule 10. Day 5-6 integration test = first canary for any drift. |
| 42 | 2-10 | Mock-to-real CoachingReport swap | `app/frontend/components/AnalyzeFlow.tsx` `buildMockReport` produces a Sarah Reynolds canned response. When Vinh's `/api/analyze` ships Day 5-6, swap `buildMockReport()` for a real `fetch()` call. If the mock and real shapes diverge in any detail (e.g., the mock has 10 forecast points but real ships 24 mini-sectors), the UI may render unexpectedly | Mock shape was hand-written, real ships from Pydantic | Day 5-6 task = swap + integration test on Sarah fixture. The mock is intentionally structurally identical to the typed contract, so TypeScript will catch most mismatches at compile time. The 10-point forecast count is documented as a placeholder; expand the mock to match Vinh's real count once the canned Sarah backend response lands. |
| 43 | observed-pre-mortem-meta | Commit subject 91a5141 = 103 chars, exceeded ≤100 cap | Long feature-list subject for the 3-component wave; manual --amend pre-push not enforced | Subject line discipline drift in long sessions | Going forward: pre-push subject-length check + amend before push when over cap. pre-submit-checks.sh has no per-subject length gate yet; consider adding as Check 21 in a future hardening pass. Acceptable as observed Day-1-EOD residual; not rewriting history mid-stream. |
| 44 | 2 | Local vitest worker pool hangs under macOS process-table contention | Concurrent reviewer agents (4 in parallel) + Next dev server + multiple sibling node procs leave the macOS process table saturated; vitest forks new node workers per test file and the new workers cannot ping back within the default 60s timeout. `[vitest-pool-runner]: Timeout waiting for worker to respond`. Test runs return "0 tests, 7 errors" with no actionable code-side signal. | macOS process-table contention; vitest fork-pool isolation cost compounded by Mac-host parallelism. CI Linux runners observed-clean (run 26222032143 on `b65e810` GREEN in 37s; run 26220883704 on `69720b8` GREEN in 39s with all 50 tests passing in the same vitest invocation that hung locally). | Wave-16 workaround: switched to `pool: "threads"` + `singleThread: true` locally. Cleanest fix: drop concurrent agents during test verification waves OR run vitest only after agents have returned. Lesson: CI green on Linux runners is the authoritative test gate; local fail under contention is non-load-bearing. | 🟡 |
| 45 | observed-galaxy-tier-pattern | Galaxy-tier Day-2 pull-forward of PLAN Day-4 to Day-11 tasks is the load-bearing project rule | D-003 + user's "we're aiming for the galaxy" directive + the discovery that each subsequent wave catches more regression surfaces than the last only because the prior wave shipped something testable | Hypothesis: galaxy-tier scope rule's real ROI is in the COMPOUNDING effect: every Day-2 pull-forward becomes a Day-3+ regression surface, which then catches Day-4+ bugs the locally-tested code never would have surfaced. Convergence-14-style discipline applied to UX (e.g. /judges) AND infra (e.g. CI) AND tests (e.g. vitest 50-test suite) AND submission prep (e.g. payload.md draft). | Going forward: every wave-end, spend ~5 min identifying the single highest-impact Day-N task that's pullable to today. Atomic-commit + ship. The COMPOUNDING test-surface is the actual galaxy-tier asset, not the individual feature ships. | 📘 (pattern-locked lesson, not a resolved failure; silent-failure-hunter wave-17 M-4 disambiguated from ✅ resolved-failure marker) |

## Accepted residual risks (ship with these on Day 12)

- ⚠ Sub-second sub-grid kinetic hallucinations inside 1-Hz aggregates remain possible (V1 physics layer). V2 Pacejka mitigation is post-NeurIPS-paper work.
- ⚠ Constant-mu friction ellipse oversimplifies wet-track scenarios. Day 5 V2 adds circuit-conditional mu lookup; full Pacejka load-dependent slip is V3.
- ⚠ apex.race domain may not be registered by Day 12 (user-action gate). Fallback metadataBase currently in code: `https://apex-race.vercel.app` (the Vercel auto-alias for the `apex-race` project name). Confirm during Day 9 HF Space + Vercel deploy.

---

## Day 2 night (2026-05-21) - additions

| # | Surface | Failure mode | Why it happens | Mitigation | Status |
|---|---------|--------------|----------------|------------|--------|
| 46 | BeMyApp banner default-placeholder risk | Submitting with the default BeMyApp "EXAMPLE PROJECT NAME, Recommended Banner Size 1920 x 600" placeholder reads as "did not ship a banner" (observed: at least one project in the live BeMyApp gallery shipped the default placeholder despite having an otherwise polished page) | Default-placeholder slot is non-blocking on the submission form, so projects ship without uploading a custom 1920x600 | Shipped editorial-magazine-cover banner Day 2 night-late via Next.js ImageResponse renderer at `app/frontend/lib/bemyapp-banner.tsx` + route handler at `app/bemyapp-banner/route.ts` + brand brief at `docs/banner-brand-brief.md` + final PNG at `deliverables/bemyapp-banner-1920x600.png`. Warm cream + Fraunces italic deliberately contrasts the universal dark-cinematic competitor banner aesthetic. | ✅ |
| 47 | Google Fonts CDN unavailability at render time | The banner renderer fetches Fraunces + IBM Plex Sans + IBM Plex Mono from `fonts.googleapis.com` at render time. If the build environment (Vercel runtime or local dev) lacks network access, the route handler 500s | Externalized font fetch keeps binary font files out of the repo (~500kb saved) but introduces a runtime dependency on Google Fonts CDN | The committed `deliverables/bemyapp-banner-1920x600.png` is the canonical artifact uploaded to BeMyApp at submission time; the route handler is iteration-only. Submission-time PNG availability does not depend on Google Fonts being reachable. | ⚠ accepted residual risk for iteration; not for submission |
| 48 | BeMyApp panel depth under-shipping vs competitor benchmark | The BeMyApp form's "What to write" prompt says "In 2-3 sentences"; an inexperienced author treats this as a constraint and ships a thin Issue + Magic Solution panel. NeuroPit + PitWall + RaceMind shipped 3-paragraph / 120-300-word panels and visibly land more weight on the judge-visible page | The form's example text is read as the requirement floor, not the example floor | Wave-21 rewrote the locked draft copy in `deliverables/bemyapp-submission-payload.md` to the NeuroPit-depth standard (~140 words Issue, ~190 words Magic Solution) with bold-key-phrase emphasis + layer-by-layer technical breakdown + "audit first, always" payoff. Canonical reference: `reference_bemyapp_submission_template.md` panel-budget guidance section. | ✅ |
| 49 | Propagation-type cold-review finding closures seed wave-N+1 drift | Each wave's closures fix "the obvious" instances of a finding then miss ~10 peripheral surfaces still carrying the pre-reformulation language. Wave-N+1's cold review re-surfaces the same finding as net-new BLOCKERs. Closure cost compounds. | The wave-23 -> wave-24 -> wave-25 -> wave-26 sequence demonstrated the pattern: single-stage QP language survived in paper title + README Mermaid + decision-log D-A + 12 other surfaces despite three closure waves naming surfaces explicitly. | Wave-25 captured the systematic rule in `feedback_propagation_full_repo_sweep.md`: when closing a propagation-type finding, grep the entire repo for the pre-reformulation language not just the surfaces named in the finding doc. Wave-26 verified the rule by sweeping wave-26 closures + finding the BeMyApp submission payload + the wave-26-touched files all in scope; em-dash + AI-tone + single-stage residual sweep all clean post-wave-26. | 📘 (pattern-locked lesson; ✅ for the rule's adoption, not a resolved failure) |
| 50 | Galaxy-tier visualization compounds judge perception | The Convergence-14 fixture grid shipped wave-26 turned the "load-bearing safety contract per D-A" claim from a verbal Q&A defense card into a visually inspectable 14-tile grid on the `/judges` page. A judge clicking /judges now sees the safety story rather than just reading about it. Same pattern: Figure 1 architecture embed in the same wave turned the Mermaid source into a rendered diagram a judge sees. | The galaxy-tier compounding rule (row 45 📘) predicts: every visual surface shipped early becomes the regression-catcher for the next wave's claim review. Wave-26 confirms: the fixture grid would have caught the wave-25 single-stage-language drift on README Mermaid + arch-spec ASCII at the wave-25 review boundary if the grid had shipped wave-25 instead. | Going forward: every cold-review wave should ask "what claim survives only in prose, and could be visualized cheaply this wave?" Each visualization shipped becomes the regression-catcher for the next claim review. | 📘 (pattern-locked lesson; ✅ for the wave-26 Convergence-14 grid landing) |

---

## Pre-Q&A defense cross-reference

Every BLOCKER + HIGH residual risk has a corresponding Q&A flashcard or rehearsed answer. See `~/.claude/projects/-Users-stephensookra-Desktop-IBM-May/memory/project_apex_qa_killshots.md` for the 5 cards. Update both this journal and the killshots memory in lockstep.

---

_Last updated: 2026-05-21 night-late by Stephen (wave-26 row 49 propagation-finding closures seed wave-N+1 drift pattern-locked + row 50 galaxy-tier visualization compounds judge perception pattern-locked)._
